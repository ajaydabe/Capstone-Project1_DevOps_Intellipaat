apiVersion: v1
kind: Service
metadata:
  name: website-service
spec:
  selector:
    app: mywebsite
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
